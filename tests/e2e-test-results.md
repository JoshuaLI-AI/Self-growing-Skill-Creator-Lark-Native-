# ASC for Lark v1.0 — E2E 测试结果报告

> **版本：** v1.0  
> **测试日期：** 2026-04-22  
> **测试方法：** 静态走查 + 模拟推演（无需真实飞书环境）  
> **测试范围：** SKILL.md 全文 ~1200 行，四阶段 + 行为约束 + 子技能模板  

---

## 一、总体评分

| 维度 | 评分 | 说明 |
|------|------|------|
| **指令完整性** | ★★★★☆ | 核心命令模板、checkpoint、判断分支齐全 |
| **逻辑一致性** | ★★★★☆ | 变量命名统一，数据流清晰，跨章节引用匹配 |
| **可执行性** | ★★★★★ | LLM 可直接按指令执行，无需额外解释 |
| **约束覆盖度** | ★★★☆☆ | C2-C8/C11 落地扎实；C1/C9/C10 偏弱 |

### 总判定：**通过 ✅（PASS with WARNINGS）**

---

## 二、各阶段验证统计

| 阶段 | 总检查项 | PASS | WARN | FAIL | Blocker | Major | Minor |
|------|----------|------|------|------|---------|-------|-------|
| **Stage 1** (§2.0–§2.9) | 17 | 15 | 2 | 0 | **0** | **0** | **2** |
| **Stage 2** (§3.0–§3.5) | 18 | 16 | 2 | 0 | **0** | **0** | **2** |
| **Stage 3** (§4.0–§4.7) | 16 | 15 | 1 | 0 | **0** | **0** | **1** |
| **子技能模板** (G1-1～G1-10) | 10 | 6 | 4 | 0 | **0** | **1** | **3** |
| **Stage 4+约束** (S4-1～S4-18) | 18 | 14 | 4 | 0 | **0** | **2** | **2** |
| **合计** | **79** | **66** | **13** | **0** | **0** | **3** | **10** |

> **关键数据：Blocker = 0 ✅ | Major = 3 ⚠️ | Minor = 10 ℹ️**

---

## 三、Bug 清单

### Blocker 级（0 个）

无。所有核心流程节点均有完整命令模板和判断条件。

---

### Major 级（3 个）

| 编号 | 来源阶段 | 问题描述 | 影响 | 建议修复方案 |
|------|----------|----------|------|-------------|
| **M-01** | T3.5 G1-10 D3 | §2.7 模板使用 `[KM_TABLE]` / `[EL_TABLE]` 占位符名，而 `subskill-skeleton.md` 使用 `[KM_TABLE_ID]` / `[EL_TABLE_ID]`。两处命名不一致可能导致 AI 执行时混淆或生成不一致的子技能文件 | 子技能生成产物可能因执行参照不同而产生字段名差异 | 统一为 `[KM_TABLE_ID]` / `[EL_TABLE_ID]`（带 `_ID` 后缀更明确表达 table_id 语义），同步修改 SKILL.md §2.7 第 399-400 行 |
| **M-02** | T3.6 C1 映射 | **C1 约束（严格按 Stage 顺序执行）仅在 §6 表格中声明，各 Stage 启动检测小节中无显式的「确认上一 Stage 已完成」断言语句**。依赖 LLM 自行理解执行顺序而非硬编码保障 | 在复杂对话或多轮交互场景下，LLM 可能跳过某 Stage 直接进入后续阶段，导致前置数据缺失 | 在 §3.0 / §4.0 首行分别增加 `**MUST 确认 Stage 1 / Stage 2 已全部完成后方可继续**` 断言；或在 §5.0 启动序列中加入前置依赖检查步骤 |
| **M-03** | T3.6 C9 映射 | **C9 约束（动态字段必须在 Stage 1 完成）仅在 §6 声明，正文中未明确定义「哪些是动态字段」以及「创建时机」的具体操作位置**。读者需自行推断 | Stage 3 写入 EL 时可能遗漏动态字段，或用户不清楚何时/如何新增领域变量 | 在 §2.5（Step 5 动态字段创建）开头增加定义语句：「以下字段称为『动态领域变量字段』，由 Step 1 访谈收集确定，必须在本 Step 创建完毕」；并在 §4.0 启动检测增加动态字段存在性检查项 |

---

### Minor 级（10 个）

| 编号 | 来源阶段 | 问题描述 | 建议 |
|------|----------|----------|------|
| **m-01** | T3.2 S1-13 | §2.7 SKILL.md 写入模板（384-420行）使用方括号占位符 `[...]` 但缺少一句显式替换指令 | 在模板前加：「将以下模板中的 `[占位符]` 替换为实际值后写入文件」 |
| **m-02** | T3.2 S1-15 | CP-S1-5（字段完整性验证）和 CP-S1-8（文件写入成功）的检测逻辑偏简略，缺少失败时的处理动作 | 补充类似 CP-S1-2 的 `→ 标记此步已完成 / → 停止并报错` 格式 |
| **m-03** | T3.3 WARN-001 | CP-S2-0 断点续传明确列举了「继续」「重建」两条路径，缺少「首次运行（KM 表空表）」的第三种路径说明 | 补充：当 `+record-list` 返回空数组时自动进入首次模式，无需询问 |
| **m-04** | T3.3 WARN-002 | §3.3d 写入后回读验证仅一句话，缺乏重试次数上限和重试策略细节 | 补充：最多重试 3 次，每次间隔 2 秒；仍失败则输出失败行 JSON 供人工排查 |
| **m-05** | T3.4 S3-4 | §4.1b 元信息收集 3 个问题中第 3 个标记「(可选)」，但检查清单描述暗示 3 个均为必答 | 将第 3 个问题改为必答并移除 `(可选)` 标记，或在检查清单中修正为「2 必答 + 1 可选」 |
| **m-06** | T3.5 G1-3~G1-6 | §2.7 模板和 skeleton 文件的 YAML 配置块占位符均缺少值类型注释（如 `[KM_TABLE]` 未标注应为 `tbl...` 格式） | 在 LARK_KNOWLEDGE_BASE 块中每个占位符后追加行内注释：`# table_id 格式 tbl...` 或 `# node_token 格式` |
| **m-07** | T3.5 G1-10 D1 | §2.7 description 使用 `"[技能描述]"` 而 skeleton 使用 `"[技能一句话描述]"`，措辞微差 | 统一为一方即可，优先级低 |
| **m-08** | T3.6 Issue 3 (C10) | C10 约束声明「大文档 >5000 字分批处理」，但正文中仅有提取粒度指导（§3.2c），缺少运行时分批读取的操作指令 | 在 §3.1c 或新增 §3.1x 补充：当文档 >5000 字时按 H2 标题分段调用 docs +fetch，每批 ≤4000 字 |
| **m-09** | T3.6 Issue 4 (n-gram) | n-gram 实际列出 A/B/C/D 四步，而文档描述为「5 步算法」，排序操作嵌入在 D 中未独立编号 | 将 D 拆分为 D（阈值判定）+ E（排序输出），或保持 A-D 四步但在描述中改为「4 步」 |
| **m-10** | T3.5 G1-10 D5/D8/D9 | skeleton 文件的 hint/el_field 占位符描述比 §2.7 更精确（如 `"[从用户语言识别此变量的提示]"` vs `"[识别提示]"`），建议对齐 | 以 skeleton 版本为准更新 §2.7 模板（skeleton 描述更明确），属纯文案优化 |

---

## 四、发布门禁 G1-G7 判定

| 门禁 | 标准 | 结果 | 判定依据 |
|------|------|------|----------|
| **G1** | 快速模式 ≤10 分钟完成建库 | ✅ 通过 | Stage 1 快速模式 Q1-Q5 五步链路完整，每步命令模板齐全，预估 3-5 分钟可行 |
| **G2** | Stage 2 提取知识写入 KM 表 | ✅ 通过 | 18 项检查 16 PASS，知识提取→去重→质检→写入全链路可用 |
| **G3** | Stage 3 提取经验写入 EL + 关联 | ✅ 通过 | 16 项检查 15 PASS，三轮对话→EL写入→link关联→冲突检测全链路可用 |
| **G4** | 子技能三级检索可执行模拟 | ✅ 通过 | 三级检索逻辑自洽（场景A/B/C推演通过），公式/阈值/降级链完整 |
| **G5** | 重复执行不产生重复资源 | ✅ 通过 | CP-S2-0/CP-S3-0 断点续传 + Stage 2 三路去重 + Stage 3 组合键去重 |
| **G6** | 无 Blocker 级 Bug | ✅ 通过 | **Blocker = 0** |
| **G7** | 文档齐全 | ✅ 通过 | SKILL.md + e2e-test-plan.md + e2e-test-results.md + 5 个模板文件 |

> **结论：G1-G7 全部通过 ✅ — 满足 v1.0 MVP 发布门禁条件**

---

## 五、模拟检索推演摘要

| 场景 | 输入 | 推演结果 | 判定 |
|------|------|----------|------|
| **A - EL 精确命中** | "甲方大额合同审核" + {谈判地位:"甲方", 规模:"800万"} | rec_A=HIGH（全部命中）/ rec_B=LOW（仅红线触发） | ✅ 符合预期 |
| **B - KM 降级** | "合同归档规范" + KM关键词="合同归档,存储期限,保密规定" | score=0.16 < 0.5 → 未命中，正确降级到 P3 | ✅ 符合预期 |
| **C - 全文降级** | EL+KM 均无命中 | 返回参考文件全文 + C11 免责标注 | ✅ 符合预期 |

三级检索逻辑自洽，评级/阈值/降级链条无矛盾。

---

## 六、C1-C11 行为约束落地映射总表

| 约束 | 严重度 | 落地状态 | 落地位置数 |
|------|--------|----------|-----------|
| **C1** Stage 顺序执行 | CRITICAL | ⚠️ 弱 | 1（仅 §6 声明） |
| **C2** 写入前确认 | CRITICAL | ✅ 强 | 3（设计原则 + §3.2d + §6） |
| **C3** docs +fetch 读取 | HIGH | ✅ 强 | 4（§3.1b + §4.1a + §5.4c + §6） |
| **C4** diff_analysis 不升 high | CRITICAL | ✅ 强 | 4（设计原则 + §3.3b + §5.4c + §6） |
| **C5** 红线强制触发 | CRITICAL | ✅ 强 | 2（§5.2b + §6） |
| **C6** 单向 EL→KM | HIGH | ✅ 强 | 3（字段定义 + 统计模板 + §6） |
| **C7** 仅飞书 URL | HIGH | ✅ 强 | 2（§3.2b + §6） |
| **C8** get_node → fetch | HIGH | ✅ 强 | 4（§3.1a + §4.1a + §5.4c + §6） |
| **C9** 动态字段 Stage 1 完成 | HIGH | ⚠️ 弱 | 1（仅 §6 声明） |
| **C10** 大文档分批 | MEDIUM | ⚠️ 弱 | 1（§6 声明 + §3.2c 间接） |
| **C11** 全文降级不捏造 | CRITICAL | ✅ 强 | 3（§5.2d + §7 异常表 + §6） |

**弱落地约束（需 Phase 4 加强）：C1 / C9 / C10**

---

## 七、Phase 4 建议修复优先级

基于本次 E2E 结果，建议 Phase 4 按以下优先级修复：

| 优先级 | 编号 | 修复内容 | 工时估计 |
|--------|------|----------|----------|
| **P0** | M-01 | 统一 §2.7 与 skeleton 的占位符命名 | 5 min |
| **P0** | M-02 | 为 C1 补充 Stage 启动断言语句 | 10 min |
| **P0** | M-03 | 为 C9 补充动态字段定义和操作位置 | 10 min |
| **P1** | m-01 ~ m-05 | 补充缺失的显式指令和路径说明 | 20 min |
| **P1** | m-06 | 补充值类型行内注释 | 10 min |
| **P2** | m-07 ~ m-10 | 文案对齐和格式优化 | 15 min |

**P0 总工时：~25 min | P1+P2 总工时：~45 min**

---

*本报告由 Phase 3 E2E 测试自动生成，覆盖 79 项检查、3 组模拟推演、11 条行为约束映射。*
